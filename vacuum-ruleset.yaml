extends: [[vacuum:oas, recommended]]
rules:
  paths-kebab-case: false
  camel-case-properties: false
  no-http-verbs-in-path: false
  # vacuum had issues parsing the document if we use the recommended structure
  path-item-refs: false
