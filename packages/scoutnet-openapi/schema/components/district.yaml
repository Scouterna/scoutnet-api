type: object
required:
  - name
  - groups
properties:
  name:
    type: string
    description: The name of the district.
  groups:
    type: object
    additionalProperties:
      $ref: './group.yaml'
